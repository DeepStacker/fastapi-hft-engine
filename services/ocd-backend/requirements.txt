# Stockify Trading Platform - OCD Backend
# 
# This service uses the root requirements.txt for dependencies.
# Install with: pip install -r ../../requirements.txt
#
# Service-specific dependencies (if any) can be added below:
# -r ../../requirements.txt

# No additional dependencies needed - all deps are in root requirements.txt
